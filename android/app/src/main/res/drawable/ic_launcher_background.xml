<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Solid splash/placeholder background only (avoid shape inside &lt;bitmap&gt;). -->
  <item android:drawable="@color/splashscreen_background" />
</layer-list>
